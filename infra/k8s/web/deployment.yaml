apiVersion: apps/v1
kind: Deployment
metadata:
  name: kikos-web
  namespace: kikos
spec:
  replicas: 2
  selector:
    matchLabels:
      app: kikos-web
  template:
    metadata:
      labels:
        app: kikos-web
    spec:
      containers:
        - name: kikos-web
          image: ghcr.io/ilannildo/kikos-web:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
          envFrom:
            - configMapRef:
                name: kikos-web-config
            - secretRef:
                name: kikos-web-secrets          
      imagePullSecrets:
              - name: regcred